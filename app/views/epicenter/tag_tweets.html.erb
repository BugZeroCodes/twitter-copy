<div>
    <h1><%= @tag.phrase %></h1>
</div>

<div class="row">
  <!-- a place for current user stats -->
  <div class='well'>
    <% @tag.tweets.order(created_at: :desc).each do |tweet| %>
        <p>
	    <%= tweet.user.name %> | <%= link_to tweet.user.username, show_user_path(id: tweet.user.id) %>
	    • <%= time_ago_in_words(tweet.created_at) %>
        </p>
	<p>
	    <%= tweet.message.html_safe %>
	</p>
  <hr />
    <% end %>
   </div>
</div>
