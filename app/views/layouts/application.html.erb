<!DOCTYPE html>
<html>
  <head>
    <title>TTS Twitter</title>
    <%= csrf_meta_tags %>
    <style type="text/css">
      .btn.btn-danger {
        background-color: red;
        border-color: darkred;
      }
      .navbar-collapse.collapse {
        background-color: black;
      }
      td {
        border: 5px groove #ffaadd;
      }
      ul.dropdown-menu {
        box-shadow: -4px 2px 20px 3px black;
      }
      .table {
        box-shadow: 9px 6px 20px 3px black;
      }
      .well {
        background-color: gray;
      }
    </style>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar-collapse collapse">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Home", root_path, class: "navbar-brand" %>
      </div>
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Make a New Tweet", new_tweet_path %></li>
          <li><%= link_to "All Users", all_users_path %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                <% if !current_user.avatar.blank? %>
                  <%= image_tag current_user.avatar.url, width: 60, height: 50, class: "user-pic-nav", style: 'border-radius: 10px;' %>
                <% end %>
                <%= current_user.username %> <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Edit Profile", edit_user_registration_path %></li>
                <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
  <% if notice != nil %>
    <p id='notice' class='alert alert-info'><%= notice %></p>
  <% end %>
  <%= yield %>
  </body>
</html>
